# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: jams
metadata:
  template: jams-staging@0.1.0

infra:
  provider: bicep
  path: infra

services:
  web:
    project: apps/web
    language: docker
    host: containerapp
    docker:
      path: Dockerfile
      context: ../..
  worker:
    project: worker
    language: docker
    host: containerapp
    docker:
      path: Dockerfile
      context: ..
