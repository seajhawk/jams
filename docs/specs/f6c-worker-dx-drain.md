# Spec F6-c: worker observability + drain mode + labeling diagnosis

Branch `f1-foundation`; push when green. Context: three live labeling attempts left no visible or persisted outcome — the provider stamped provider_versions but names stayed rule-based, worker logs show nothing about why, and the infinite poll loop looks identical to a hang from outside. Fix the observability gap first, then the labeling issue becomes trivially visible.

1. **Drain mode:** `uv run jams-worker --drain` processes messages until the queue is empty, then exits 0 with a one-line summary per run handled. Default (no flag) keeps the infinite loop. All local/live checks and docs switch to --drain.
2. **Structured, unbuffered logging:** line-per-event with flush (or PYTHONUNBUFFERED in the console script): worker start (config summary incl. whether OPENROUTER key present and JAMS_LABELING_MODEL value), message claim, each stage start/finish with duration, and a MANDATORY one-line outcome per provider — for segment_labeling specifically: `applied N renames, M merges, usage in/out tokens` or `skipped:<reason>` or `rejected:<reason>` or `error:<http status/exception class>`.
3. **Durable provider outcomes:** add `provider_results` jsonb to analysis_runs (Drizzle migration) — each provider writes its outcome object (status, reason, counts, token usage for labeling). Web: no UI yet; just persisted.
4. **Diagnose + fix the labeling miss:** with the above in place, invoke the segment_labeling provider directly (script or test harness) against run ec6ebbb9-3ce1-4c3b-90f1-04eb7510fa71's persisted data with OPENROUTER_API_KEY (from Windows user env via powershell GetEnvironmentVariable('OPENROUTER_API_KEY','User')) and JAMS_LABELING_MODEL=openai/gpt-oss-120b. Find why nothing applied (candidates: another 429, request/response shape mismatch vs the model's output, validation rejecting valid names, a guard misfiring). Fix the actual bug. If the model's JSON needs coaxing, tighten the prompt/response_format — validation rules stay as specced.
5. **Prove it end-to-end:** fresh flagged run for video 090e7f5f-e959-4f4a-8b9a-75933b60032f via enqueue_local (extend it to accept `--config '{"llm_labeling":{"enabled":true}}'` instead of requiring SQL pokes), `uv run jams-worker --drain` → exits on its own; report the LLM segment names actually persisted, the provider_results entry, and token usage/cost.
6. `uv run pytest` + `ruff` green (drain-mode + logging + provider_results covered by tests; labeling tests stay mocked). User dirty files untouched. Finish: summary + scorecard row.
